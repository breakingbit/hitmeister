version: '0.0.{build}'

configuration:
- Debug
# - Release

before_build:
- appveyor-retry dotnet restore -v Minimal

build_script:
- dotnet build .\**\*.csproj

test_script:
- dotnet test

cache:
- '%USERPROFILE%\.nuget\packages'